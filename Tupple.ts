//Tupple: combo of two(multiple) types, but limited items

let person1: [string, number] = ["shreyash", 22];
console.log(person1);

let tupple2 : [string, number, number]
tupple2 = ['Hello',12,45]
console.log(tupple2)